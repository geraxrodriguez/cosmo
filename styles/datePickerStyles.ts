import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    datePickerContainer: {
        alignItems: 'center',
        marginBottom: 30,

    },
    button: {
        padding: 0,
        margin: 0,
    },
    buttonText: {
        color: '#ffab00',
        fontSize: 18,
        fontWeight: 'bold',
        textAlign: 'center',
        marginBottom: -5,
    }
})

export default styles;

